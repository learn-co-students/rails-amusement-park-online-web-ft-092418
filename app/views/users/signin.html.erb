<%= form_for :user, url: login_path do |f| %>
    <%= f.label :name, "Your name: " %>
    <%= f.collection_select :name, @users, :name, :name %> <br>

    <%= f.label :password, "password" %>
    <%= f.password_field :password %> <br>

    <%= f.submit "Sign In" %>
<% end %>